<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts
        PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd" >
<struts>
    <!-- struts2 的 action 必须放在一个指定的包空间下定义  -->
    <package name="default" extends="struts-default">
        <!--  定义处理请求 URL 为 reg.action 的 Action -->
        <!--reg.action,接受到用户的请求后，创建RegAction这个类的对象，进行相应的处理-->
        <action name="reg" class="regAction"><!--Spring中RegAction的id-->
            <!--  定义处理结果字符串和资源之间的映射关系  -->
            <result name="success">/index.jsp</result>
            <!--<result name="error">/error.jsp</result>-->
        </action>
    </package>
</struts >